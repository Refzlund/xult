---
"xult": minor
---

feat: added `funcJSON` which returns `Result.JSON` instead of a `Result` (for environments such as Cloudflare Workers) See [this Workers SDK issue #11388](https://github.com/cloudflare/workers-sdk/issues/11388)
